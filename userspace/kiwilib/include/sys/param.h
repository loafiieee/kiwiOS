#ifndef KIWILIB_SYS_PARAM_H
#define KIWILIB_SYS_PARAM_H

#include "limits.h"

#ifndef MIN
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#endif

#ifndef MAX
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#endif

#endif // KIWILIB_SYS_PARAM_H
